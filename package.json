{
  "name": "zen-sci-monorepo",
  "version": "0.0.1",
  "private": true,
  "description": "ZenSci — Knowledge-Work Forge: MCP servers for publication-ready document conversion",
  "license": "Apache-2.0",
  "scripts": {
    "build": "pnpm -r run build",
    "build:apps": "pnpm --filter './servers/*/app' run build",
    "build:all": "pnpm run build && pnpm run build:apps",
    "test": "pnpm -r run test",
    "test:apps": "pnpm --filter './servers/*/app' run test",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.1"
  }
}
